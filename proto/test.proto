syntax = "proto3";

package test.v1;

// 这是一个测试服务
service TestService {
  // 测试方法
  rpc Test(TestRequest) returns (TestResponse);
}

// 测试请求
message TestRequest {
  string message = 1;
}

// 测试响应
message TestResponse {
  string message = 1;
} 